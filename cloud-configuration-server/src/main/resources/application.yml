server:
  port: 9200

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri:
          clone-on-start: true

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:9000/eureka/
  instance:
    hostname: localhost